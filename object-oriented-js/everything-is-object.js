/**
 * Everything in JS is object and we can find __proto__ for every object
 * Insepect all of these in chrome console and seach for proto
 * like : 
 * a.__proto__
 * b.__proto__
 * c.__proto__
 */

var a = {}
var b = function() {}
var c = []

/**
 * The base prototype for all objects is 'Object' class in JavaScript, which is a built in class.
 */